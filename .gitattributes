# Shell scripts must stay LF in the repo AND on Windows checkouts.
# CRLF line endings break the shebang (#!/bin/sh\r) and the container
# fails with "exec /entrypoint.sh: no such file or directory".
*.sh text eol=lf
