# Locally maintained IP blocklist, one per line: <ip> <optional description>
# Lines starting with # are comments.
#
# Seed this with real threat intel over time (exports from AbuseIPDB,
# Feodo Tracker's IP blocklist, URLhaus, etc.) — this file is what keeps
# IOC correlation working even with no internet access during a demo.
#
# 203.0.113.0/24 is a reserved TEST-NET range (RFC 5737) — safe to use
# as a demo/validation entry since it will never route to anything real.
203.0.113.66 Demo/test entry — reserved TEST-NET range, safe for pipeline validation
